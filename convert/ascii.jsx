import constants from './constants.jsx';

const toBin = (input) => {
  // Get int from JSON
  // convert int to bin
}

const toDec = (input) => {
  // sdfasfdasd
}

const toHex = (input) => {
  // sdfasfdasd
}

const toBase64 = (input) => {
  // sdfasfdasd
}

export default {
  toBin: toBin,
  toDec: toDec,
  toHex: toHex,
  toBase64: toBase64,
};
